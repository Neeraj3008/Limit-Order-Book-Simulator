# Backend
PORT=8000
# Use a managed PostgreSQL URL in production. SQLite is suitable only for local development.
DATABASE_URL=sqlite:///./exchange.db
# Comma-separated browser origins, with no trailing slash.
CORS_ORIGINS=http://localhost:5173
